export enum PlayStatus {
  WAITING = 'waiting',
  PLAYING = 'playing',
  ENDED = 'ended',
  AVAILABLE = 'available'
}

export enum GameStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  BUILDING = 'building'
}

export enum PlayerStatus {
  PLAYING = 'playing',
  OUT = 'out',
  WAITING = 'waiting',
  REJECT = 'reject'
}
